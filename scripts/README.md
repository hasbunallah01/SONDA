# `scripts/` — Operational scripts

One-off Node/TS scripts that support development, CI, or ops. Not for application code.

## Planned

- `seed.ts` — Seed local DB
- `reset-db.ts` — Drop and recreate local DB
- `smoke.ts` — End-to-end smoke test against a deployed env
