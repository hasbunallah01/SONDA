# `styles/` — Global styles

Global stylesheets beyond `app/globals.css`. Keep this folder thin — most styling should live in Tailwind utilities.

## Current contents

- (empty in this task — Tailwind base is set up in `app/globals.css`)

> Avoid dumping large CSS files here. Prefer Tailwind + component-local styles.
