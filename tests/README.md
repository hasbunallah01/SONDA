# `tests/` — Tests

End-to-end and integration tests. Unit tests live next to the code they test (`*.test.ts` co-located with source).

## Planned

- `e2e/` — Playwright end-to-end tests
- `integration/` — Service-level integration tests
- `fixtures/` — Shared test data
